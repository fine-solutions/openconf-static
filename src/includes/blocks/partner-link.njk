{% macro partner(parentClass, item) %}
  <a class="{{ parentClass }} partner-link" href="{{ item.url }}" title="{{ item.title }}">
    <img class="partner-link__image" src="{{ item.image }}" alt="">
  </a>
{% endmacro %}
