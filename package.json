{
  "private": true,
  "name": "openconf-static",
  "description": "A static site for any conferences based on 11ty engine.",
  "author": "Meetings Ecosystem",
  "license": "GPL-3.0-only",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --incremental --serve --quiet",
    "build": "eleventy --quiet",
    "lint:editorconfig": "editorconfig-checker"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "editorconfig-checker": "^6.0.1"
  }
}
